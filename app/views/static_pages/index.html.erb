<div class="container-fluid">
  <div class="row">
    <div class="col">
      <h1>Demonsional!</h1>
      <h2>Demo like it's your job</h2>
      <h3>First you need to self register</h3>
      <h4>Next you can setup your data</h4>
      <h5>Finally you can create your demo environment</h5>
      <p>
        What does creating your demo environment do? It will create a wondrous environment that allows you to tell a
        compelling story of value.
      </p>
    </div>
    <div class="col">
      <h1>Dashboard</h1>
   <!--   <h2>Current Shipping Orders: <%#= current_user.shipping_orders.count %></h2>
      <h2>Current Enterprises: <%#= current_user.enterprises.count %></h2>
      <%# if Profile.cust_acct(current_user).blank? %>
        <h2>Parent Enterprise: <%#= link_to 'Configuration Needed', "users/edit" %></h2>
      <%# else %>
        <h2>Parent Enterprise: <%#= link_to Profile.cust_acct(current_user), profile_path(Profile.id(current_user)) %></h2>
      <%# end %>
      <h3>Contracts: <%#= current_user.contracts.count %></h3>
      <h3>Rates: <%#= current_user.rates.count %></h3>-->
      <% begin %>
        <h3>Active Profile: <%= current_user.profiles.cust_acct(current_user) %></h3>
      <% rescue %>
        <h3><%= link_to 'Need to setup profiles', new_profile_path %></h3>
      <% end %>
    </div>
  </div>
  <h2>MMO in the cloud status</h2>
  <%= @mmo_status.html_safe %>
</div>